<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/_head", { cssFiles: ["auth.css"], jsFiles}) %>
  <body>
    <main>
      <form>
        <div class="header"></div>
        <h2><% if (login) { %> Login <% } else { %> Register <% } %></h2>
        <% if (!login) { %>
        <div class="input-container">
          <input type="text" name="name" id="name" placeholder=" " required />
          <label for="name">Username</label>
        </div>
        <% } %>
        <div class="input-container">
          <input
            type="email"
            name="email"
            id="email"
            placeholder=" "
            required
          />
          <label for="email">Email</label>
        </div>
        <div class="input-container">
          <input
            type="password"
            name="passwd"
            id="passwd"
            placeholder=" "
            required
          />
          <label for="passwd">Password</label>
        </div>
        <button type="submit">
          Submit
          <div class="left"></div>
          <div class="top"></div>
          <div class="right"></div>
          <div class="bottom"></div>
        </button>
        <% if (login) { %>
        <p>Don`t have an account? <a href="register">Register now</a></p>
        <% } %>
      </form>
      <div id="msg"></div>
    </main>
  </body>
</html>
